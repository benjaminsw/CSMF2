# SEQREF-CFG-FM v0.1 -- fm_refine_armA
# LIFETIME: KEEP
# Level-3 flow-matching refiner, Arm A: cond = [y_up, x1, Aᵀr1] (3ch).
# Frozen chain: NSF seed0 -> NICE Run-3. Gates are module constants (plan §8).
refiner: {hidden: 64, depth: 4, t_embed_dim: 64, g_max: 0.5, g_init: 0.05}
flow_matching: {path: linear, k_train: 4, k_eval: 8, integrator: euler}
base:
  run_dir: ./fashion_seqref/results/bases/nsf/nsf_s4_n0.10_seed0_079f6439b047
  n_post: 16
stage1:
  run_dir: ./fashion_seqref/results/refiners/nice_refine_s4_n0.10_seed0_531bdbf37f3f
train:
  seed: 0
  epochs: 60
  batch_size: 128
  lr: 0.0003
  grad_clip: 5.0
  charbonnier_eps: 0.001
  lambda_img: 0.1
  lambda_budget: 0.001
  budget_form: g_dx
  early_stop_patience: 15
output:
  root: ./fashion_seqref/results/fm_refiners
